- if current_page
  %nav.sub_page
    - sub_pages.each do |sub_page|
      = link_to sub_page.name.titleize, "#{current_page.name}/#{sub_page.name}"
  %h2= current_page.name.titleize
  = format(current_page.body)
